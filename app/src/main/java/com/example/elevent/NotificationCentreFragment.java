package com.example.elevent;

import android.os.Bundle;
import android.widget.Button;
import android.widget.ListView;
import android.widget.TextView;
import androidx.appcompat.app.AppCompatActivity;

import com.google.android.material.floatingactionbutton.FloatingActionButton;

public class NotificationCentreFragment extends AppCompatActivity {

    /*

    tried to initialize but not too sure if its right so gonna comment it all out T-T

    private Button addNotifButton;
    private TextView notifCentreView;
    private ListView listOfNotifs;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.fragment_notificationcentre);

        // Find views by their respective IDs
        addNotifButton = findViewById(R.id.add_notif_button);
        notifCentreView = findViewById(R.id.notif_centre_textview);
        listOfNotifs = findViewById(R.id.list_of_notifs);

        // Now you can use these views as needed
        // For example, you can set click listeners for the button
        addNotifButton.setOnClickListener(v -> {
            new NotificationCentreFragment().show(getSupportFragmentManager());
        });

        // You can also set data to your TextView and ListView
        // attendeeListTextView.setText("Attendees List");
        // Set adapter to ListView
        // Example: listOfAttendees.setAdapter(yourAdapter);
    }*/
}
